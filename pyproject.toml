[project]
name = "CampusTask"
version = "0.3.0"
description = "校园任务清单 - 命令行任务管理工具"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []
license = {text = "MIT"}

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]