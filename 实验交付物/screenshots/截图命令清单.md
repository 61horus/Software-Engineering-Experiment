# 截图命令清单

> 逐条复制到终端执行，每条截图一张。截图保存到本目录。

---

## 实验三

```bash
cd D:\code\python\experiment
python -m pytest tests/ -v
```
→ 保存为 `实验三_测试全部通过.png`

---

## 实验七

```bash
cd D:\code\python\experiment

# 1. 版本号
python -m campus_task --version

# 2. 帮助信息
python -m campus_task --help

# 3. 全命令连贯演示
python -m campus_task add "截图测试任务" --deadline 2026-07-01 --priority high
python -m campus_task list
python -m campus_task list pending
python -m campus_task search 截图
python -m campus_task overdue
python -m campus_task export screenshots\tasks_demo.csv
python -m campus_task done 1
```
→ 分别保存为 `实验七_version.png`、`实验七_help.png`、`实验七_全命令演示.png`

---

## 实验八

```bash
cd D:\code\python\experiment
python -m campus_task.ai_harness
```
交互输入：
```
>>> 帮我添加一个复习软件工程的任务
>>> 列出我还没完成的任务
>>> 把第2个任务标记为完成
>>> 删除所有任务
>>> （回车退出）
```
→ 保存为 `实验八_harness交互.png`

---

## 实验六（需浏览器）

1. 打开 https://github.com/61horus/Software-Engineering-Experiment/actions
2. 找一次失败的工作流 → `实验六_CI红灯.png`
3. 找一次成功的工作流 → `实验六_CI绿灯.png`

---

## 实验四（最后补）

```bash
cd D:\code\python\experiment
git log --oneline --graph --all
```
→ 保存为 `实验四_分支图.png`
